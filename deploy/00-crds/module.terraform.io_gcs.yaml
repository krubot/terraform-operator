
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: gcs.module.terraform.io
spec:
  additionalPrinterColumns:
  - JSONPath: .status.state
    description: Description of the current state
    name: State
    type: string
  - JSONPath: .status.phase
    description: Description of the current phase
    name: Phase
    type: string
  group: module.terraform.io
  names:
    kind: GCS
    listKind: GCSList
    plural: gcs
    shortNames:
    - mod
    singular: gcs
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: GCS is the Schema for the GCSs API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: GCSSpec defines the desired state of GCS
          properties:
            name:
              description: GCP bucket name
              type: string
            project:
              description: GCP bucket project
              type: string
            source:
              description: Kubernetes namespace GCS source
              enum:
              - /var/lib/modules/gcp/gcs/
              type: string
          required:
          - name
          - project
          - source
          type: object
        status:
          description: GCS status defines the status of GCS
          properties:
            phase:
              description: The current phase of the terraform workflow
              type: string
            state:
              enum:
              - Success
              - Failure
              type: string
          required:
          - phase
          - state
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
